# Helps cleanup the data pack when done with it.
# Should be ran manually with `/function locator_bar_colors:uninstall`
# immediately before removing the data pack.

scoreboard objectives remove locator_bar
